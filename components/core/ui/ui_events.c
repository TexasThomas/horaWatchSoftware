#include "ui.h"
#include "app.h"

/*===========================================================================
 * Event Handlers
 *===========================================================================*/

void ui_events_init(ui_handle_t *ui)
{
    // No button events needed for this watch face
    // Add touch/gesture handlers here if needed later
    (void)ui;
}
